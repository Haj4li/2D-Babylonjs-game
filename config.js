// config.js

const GameConfig = {
  DESIGN_HEIGHT: 360,  // virtual screen height in game world units
  SPRITE_SIZE: 64,     // for scaling sprites correctly
  PLAYER_SPEED: 5      // movement per frame
};
